{{define "login"}}
{{template "nav_elsewhere"}}
    <div class="container central_view">
		<div class="row justify-content-center">
			<p>Bitte gebe deine Zugangsdaten ein</p>
		</div>
		<div class="row">
			<form class="col w-100 text-center" action="index_logged_in.html">
				<div class="form-group">
					<input type="text" id="username" class="form-control bc_primary" placeholder="Benutzername" name="username" required>
					<div class="valid-feedback">OK</div>
					<div class="invalid-feedback">Es muss ein korrekter Benutzername eingegeben werden.</div>
				</div>
				<div class="form-group">
					<input type="password" id="password" class="form-control bc_primary" placeholder="Passwort" name="password" required>
					<div class="valid-feedback">OK</div>
					<div class="invalid-feedback">Dieses Feld muss ausgefüllt sein.</div>
				</div>
				<button type="submit" class="btn bg_primary_flashlight w-75 mt-4">Login</button>
			</form>
		</div>
	</div>
{{template "footer"}}
{{end}}